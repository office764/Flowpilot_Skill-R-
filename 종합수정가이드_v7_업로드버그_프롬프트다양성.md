# 종합 수정 가이드 v7 — Google Drive 업로드 버그 + B-Roll 프롬프트 다양성
## FlowPilot | 2026.03.05

---

## 문제 요약

| # | 문제 | 증상 | 근본 원인 |
|:---:|------|------|----------|
| **1** | Google Drive에 1개만 업로드됨 | 5개 B-Roll 영상이 생성됐는데 1개만 업로드 | 최종 결과 집계 코드 버그 2개 |
| **2** | B-Roll 5개가 전부 제품샷 | 씬 다양성 없음, 맥락 흐름 없음 | PRODUCT_KEYWORDS가 너무 넓어서 5개 전부 IMAGE_2_VIDEO로 분류 + 서브에이전트 broll_prompt가 제품 중심으로만 생성 |

---

# 문제 1: Google Drive에 1개만 업로드되는 버그

## 근본 원인 분석 (2개)

### 원인 A: `$('B-Roll 결과 조회').all()` — SplitInBatches 루프 밖에서 호출하면 마지막 1개만 나온다

n8n의 핵심 동작 원리:

- `B-Roll 결과 조회` 노드는 SplitInBatches 루프 **안에서** 5번 실행된다 (item 1, 2, 3, 4, 5 순서)
- 각 실행마다 이전 실행의 OUTPUT이 **덮어씌워진다**
- 루프가 끝나고 "done" 포트를 통해 `최종 결과 집계`로 올 때, `$('B-Roll 결과 조회').all()`은 **마지막 실행(item 5)의 데이터 1개만** 반환한다

이것이 brollPollItems에 1개만 들어가서, broll_clips 중 video_url이 있는 것이 1개뿐인 이유다.

### 원인 B: 노드명 불일치 — `$('토킹 클립 결과 조회')` 는 존재하지 않는다

현재 최종 결과 집계 코드:
```javascript
talkingPollItems = $('토킹 클립 결과 조회').all();
```

실제 워크플로우의 노드 이름:
```
토킹 영상 결과 조회
```

코드가 `토킹 클립 결과 조회`를 찾다가 실패 → catch에서 `InfiniteTalk 결과 조회`를 찾다가 또 실패 → talkingPollItems = [] (빈 배열)

결과: talking_clips의 video_url이 전부 빈 문자열 → Code in JavaScript2에서 talking 클립이 전부 필터링됨 → B-Roll만 업로드 대상 → 그마저도 1개만 URL 있음 → **1개만 업로드**

---

## 수정 방법: 최종 결과 집계 코드 전체 교체

### 핵심 변경점

| 변경 전 | 변경 후 | 이유 |
|---------|---------|------|
| `$('B-Roll 결과 조회').all()` | SplitInBatches의 `$input.all()` 활용 + context 변수 저장 | 루프 밖에서 루프 안의 데이터 수집 불가 → 대안 전략 필요 |
| `$('토킹 클립 결과 조회')` | `$('토킹 영상 결과 조회')` | 실제 노드명으로 수정 |

### 새로운 전략: "B-Roll 결과를 루프 안에서 누적 저장"

SplitInBatches 루프 밖에서 루프 안 노드의 전체 데이터를 가져올 수 없다. 이 문제를 해결하려면 **워크플로우 구조를 수정**해야 한다.

#### 방법: B-Roll 결과를 Static Data에 누적 저장

n8n에는 `$getWorkflowStaticData('global')` 이라는 기능이 있다. 워크플로우 실행 중 데이터를 누적 저장할 수 있다.

---

### Step 1: IF 노드 TRUE 출력 뒤에 "B-Roll 결과 저장" Code 노드 추가

1. 캔버스에서 **+** 버튼 → 검색: **Code** → Code 노드 추가
2. 노드 이름을 `B-Roll 결과 저장`으로 변경
3. Language: **JavaScript**
4. 아래 코드를 붙여넣기:

```javascript
// B-Roll 결과를 Static Data에 누적 저장
const staticData = $getWorkflowStaticData('global');

// 첫 번째 item이면 배열 초기화
if (!staticData.brollResults) {
  staticData.brollResults = [];
}

// 현재 item의 B-Roll 결과 조회 데이터를 저장
const currentData = $input.first().json;
staticData.brollResults.push(currentData);

// 확인용 로그
return [{
  json: {
    saved_index: staticData.brollResults.length,
    message: 'B-Roll 결과 ' + staticData.brollResults.length + '번째 저장 완료',
    data: currentData
  }
}];
```

### Step 2: 연결 변경

**변경 전:**
```
IF → (TRUE) → B-Roll 순차 생성 루프
```

**변경 후:**
```
IF → (TRUE) → B-Roll 결과 저장 → B-Roll 순차 생성 루프
IF → (FALSE) → B-Roll 추가 대기 60초 → B-Roll 결과 조회 (재폴링)
```

즉:
1. IF 노드의 **TRUE** 출력 → `B-Roll 결과 저장` 노드로 연결
2. `B-Roll 결과 저장` 노드의 출력 → `B-Roll 순차 생성 루프` 입력으로 연결
3. IF 노드의 **FALSE** 출력 → `B-Roll 추가 대기 60초` (기존 v6 가이드대로)

### Step 3: 최종 결과 집계 코드 전체 교체

`최종 결과 집계` 노드를 더블클릭 → 코드 영역 클릭 → **Ctrl+A** → 아래 코드를 **Ctrl+V**:

```javascript
// ============================================
// 최종 결과 집계 — v7 (2026.03.05)
// Static Data에서 B-Roll 결과를 가져오는 방식
// ============================================

// 1) 디렉터 AI 원본 5개 item
const directorItems = $('Code in JavaScript1').all();

// 2) 대본 파싱에서 브랜드/제품 정보
const scriptData = $('대본 파싱').first().json;

// 3) ★ Static Data에서 B-Roll 결과 가져오기 (루프 안에서 누적 저장한 것)
const staticData = $getWorkflowStaticData('global');
const brollResults = staticData.brollResults || [];

// 4) 토킹 영상 결과 조회에서 영상 URL 가져오기
//    ★ 노드명 수정: '토킹 클립 결과 조회' → '토킹 영상 결과 조회'
let talkingPollItems = [];
try {
  talkingPollItems = $('토킹 영상 결과 조회').all();
} catch(e) {
  talkingPollItems = [];
}

// ── URL 추출 함수 ──
function extractVideoUrl(pollData) {
  if (!pollData) return '';
  const d = pollData.json || pollData;
  try {
    // 경로 A: d.data.response.resultUrls[0]
    if (d.data && d.data.response && d.data.response.resultUrls && d.data.response.resultUrls[0]) {
      return d.data.response.resultUrls[0];
    }
    // 경로 B: d.response.resultUrls[0]
    if (d.response && d.response.resultUrls && d.response.resultUrls[0]) {
      return d.response.resultUrls[0];
    }
    // 경로 C: d.data.response.resultUris[0]
    if (d.data && d.data.response && d.data.response.resultUris && d.data.response.resultUris[0]) {
      return d.data.response.resultUris[0];
    }
    // 경로 D: d.response.resultUris[0]
    if (d.response && d.response.resultUris && d.response.resultUris[0]) {
      return d.response.resultUris[0];
    }
    // 경로 E: resultJson 파싱
    const rj = d.data?.resultJson || d.resultJson || '';
    if (rj) {
      const parsed = typeof rj === 'string' ? JSON.parse(rj) : rj;
      return parsed.video?.url || parsed.url || parsed.output?.[0] || '';
    }
  } catch(e) {
    return '';
  }
  return '';
}

// ── 토킹 클립 5개 조립 ──
const talkingClips = [];
for (let i = 0; i < directorItems.length; i++) {
  const dir = directorItems[i].json;
  const videoUrl = extractVideoUrl(talkingPollItems[i]);
  talkingClips.push({
    segment_id: dir.timeline_id,
    section: dir.section,
    time_range: dir.time_range,
    tts_text: dir.tts_text,
    video_url: videoUrl,
    status: videoUrl ? 'completed' : 'missing'
  });
}

// ── B-Roll 클립 5개 조립 (Static Data에서 가져온 데이터 사용) ──
const brollClips = [];
for (let i = 0; i < directorItems.length; i++) {
  const dir = directorItems[i].json;
  const videoUrl = extractVideoUrl(brollResults[i]);
  brollClips.push({
    broll_id: 'broll_' + (i + 1),
    broll_name: dir.broll_name || ('B-Roll ' + (i + 1)),
    section: dir.section,
    time_range: dir.time_range,
    video_url: videoUrl,
    status: videoUrl ? 'completed' : 'missing'
  });
}

// ── 집계 ──
const talkingOk = talkingClips.filter(c => c.status === 'completed').length;
const brollOk = brollClips.filter(c => c.status === 'completed').length;

// ★ Static Data 초기화 (다음 실행을 위해)
staticData.brollResults = [];

return [{
  json: {
    brand: scriptData.brand || '',
    product: scriptData.product || '',
    price: scriptData.price || '',
    full_script: scriptData.script?.full_script || directorItems[0]?.json.full_script || '',
    talking_clips: talkingClips,
    broll_clips: brollClips,
    totals: {
      talking: talkingOk + '/5',
      broll: brollOk + '/5'
    },
    status: (talkingOk === 5 && brollOk === 5) ? 'ALL_COMPLETE' : 'PARTIAL',
    next_step: (talkingOk === 5 && brollOk === 5)
      ? '전체 완료. 영상 편집 진행 가능.'
      : 'URL 누락 항목 있음. missing 항목 확인 후 재실행.'
  }
}];
```

### Step 4: 토킹 영상 쪽도 같은 문제가 있는지 확인

토킹 영상도 SplitInBatches 루프로 처리한다면, `$('토킹 영상 결과 조회').all()`도 마지막 1개만 반환할 수 있다.

현재 워크플로우 구조를 보면:
- `토킹 클립 순차 생성 루프` (SplitInBatches) → 토킹 아바타 생성 → 대기 → `토킹 영상 결과 조회`

이 경우 토킹 쪽도 Static Data로 누적 저장해야 한다.

**토킹 영상 결과 저장 노드 추가:**

1. **+** 버튼 → 검색: **Code** → Code 노드 추가
2. 노드 이름: `토킹 영상 결과 저장`
3. Language: **JavaScript**
4. 코드:

```javascript
// 토킹 영상 결과를 Static Data에 누적 저장
const staticData = $getWorkflowStaticData('global');

if (!staticData.talkingResults) {
  staticData.talkingResults = [];
}

const currentData = $input.first().json;
staticData.talkingResults.push(currentData);

return [{
  json: {
    saved_index: staticData.talkingResults.length,
    message: '토킹 영상 결과 ' + staticData.talkingResults.length + '번째 저장 완료',
    data: currentData
  }
}];
```

5. 연결: `토킹 영상 결과 조회` → `토킹 영상 결과 저장` → `토킹 클립 순차 생성 루프` (루프 반복)

그리고 최종 결과 집계에서 토킹 데이터도 Static Data에서 가져오도록 수정 (위 코드에서 이미 반영됨 — 아래 참조):

최종 결과 집계 코드의 토킹 부분을 아래로 교체:

```javascript
// 4) ★ Static Data에서 토킹 결과도 가져오기
const talkingResults = staticData.talkingResults || [];

// 토킹 클립 5개 조립에서:
const videoUrl = extractVideoUrl(talkingResults[i]);  // talkingPollItems[i] 대신
```

**최종 결과 집계 코드 (토킹도 Static Data 사용하는 최종 버전):**

```javascript
// ============================================
// 최종 결과 집계 — v7.1 (2026.03.05)
// B-Roll + 토킹 모두 Static Data에서 가져오기
// ============================================

const directorItems = $('Code in JavaScript1').all();
const scriptData = $('대본 파싱').first().json;

// ★ Static Data에서 양쪽 다 가져오기
const staticData = $getWorkflowStaticData('global');
const brollResults = staticData.brollResults || [];
const talkingResults = staticData.talkingResults || [];

function extractVideoUrl(pollData) {
  if (!pollData) return '';
  const d = pollData.json || pollData;
  try {
    if (d.data && d.data.response && d.data.response.resultUrls && d.data.response.resultUrls[0]) {
      return d.data.response.resultUrls[0];
    }
    if (d.response && d.response.resultUrls && d.response.resultUrls[0]) {
      return d.response.resultUrls[0];
    }
    if (d.data && d.data.response && d.data.response.resultUris && d.data.response.resultUris[0]) {
      return d.data.response.resultUris[0];
    }
    if (d.response && d.response.resultUris && d.response.resultUris[0]) {
      return d.response.resultUris[0];
    }
    const rj = d.data?.resultJson || d.resultJson || '';
    if (rj) {
      const parsed = typeof rj === 'string' ? JSON.parse(rj) : rj;
      return parsed.video?.url || parsed.url || parsed.output?.[0] || '';
    }
  } catch(e) {
    return '';
  }
  return '';
}

// ── 토킹 클립 5개 ──
const talkingClips = [];
for (let i = 0; i < directorItems.length; i++) {
  const dir = directorItems[i].json;
  const videoUrl = extractVideoUrl(talkingResults[i]);
  talkingClips.push({
    segment_id: dir.timeline_id,
    section: dir.section,
    time_range: dir.time_range,
    tts_text: dir.tts_text,
    video_url: videoUrl,
    status: videoUrl ? 'completed' : 'missing'
  });
}

// ── B-Roll 클립 5개 ──
const brollClips = [];
for (let i = 0; i < directorItems.length; i++) {
  const dir = directorItems[i].json;
  const videoUrl = extractVideoUrl(brollResults[i]);
  brollClips.push({
    broll_id: 'broll_' + (i + 1),
    broll_name: dir.broll_name || ('B-Roll ' + (i + 1)),
    section: dir.section,
    time_range: dir.time_range,
    video_url: videoUrl,
    status: videoUrl ? 'completed' : 'missing'
  });
}

const talkingOk = talkingClips.filter(c => c.status === 'completed').length;
const brollOk = brollClips.filter(c => c.status === 'completed').length;

// ★ Static Data 초기화 (다음 워크플로우 실행을 위해)
staticData.brollResults = [];
staticData.talkingResults = [];

return [{
  json: {
    brand: scriptData.brand || '',
    product: scriptData.product || '',
    price: scriptData.price || '',
    full_script: scriptData.script?.full_script || directorItems[0]?.json.full_script || '',
    talking_clips: talkingClips,
    broll_clips: brollClips,
    totals: {
      talking: talkingOk + '/5',
      broll: brollOk + '/5'
    },
    status: (talkingOk === 5 && brollOk === 5) ? 'ALL_COMPLETE' : 'PARTIAL',
    next_step: (talkingOk === 5 && brollOk === 5)
      ? '전체 완료. 영상 편집 진행 가능.'
      : 'URL 누락 항목 있음. missing 항목 확인 후 재실행.'
  }
}];
```

---

## 문제 1 수정 체크리스트

- [ ] `B-Roll 결과 저장` Code 노드 추가 (IF TRUE → B-Roll 결과 저장 → B-Roll 순차 생성 루프)
- [ ] `토킹 영상 결과 저장` Code 노드 추가 (토킹 영상 결과 조회 → 토킹 영상 결과 저장 → 토킹 클립 순차 생성 루프)
- [ ] `최종 결과 집계` 코드를 v7.1 코드로 전체 교체
- [ ] 워크플로우 저장
- [ ] 테스트 실행 후 최종 결과 집계 OUTPUT 확인:
  - talking_clips: 5개, video_url 전부 http로 시작하는지
  - broll_clips: 5개, video_url 전부 http로 시작하는지
- [ ] Google Drive에 10개 파일 (talking 5 + broll 5) 업로드되는지 확인

---

## 완성 후 전체 연결 구조도

```
[B-Roll 순차 생성 루프] ←────────────────────────────────┐
     ↓ (Port 1: 루프 진행)                                │
[B-Roll 영상 생성 (Veo 3.1)]                              │
     ↓                                                    │
[B-Roll 대기 120초]                                       │
     ↓                                                    │
[B-Roll 결과 조회] ←──────────────────────┐               │
     ↓                                   │               │
[IF (response 있는가?)]                   │               │
     ├── TRUE → [B-Roll 결과 저장] ───────┼───────────────┘
     └── FALSE → [B-Roll 추가 대기 60초] ─┘

[B-Roll 순차 생성 루프] → (Port 0: 루프 완료) → [최종 결과 집계]
```

토킹 쪽도 동일:
```
[토킹 클립 순차 생성 루프] ←──────────────────────┐
     ↓ (Port 1: 루프 진행)                        │
[토킹 아바타 생성 (InfiniteTalk)]                  │
     ↓                                            │
[토킹 영상 대기 120초]                             │
     ↓                                            │
[토킹 영상 결과 조회]                              │
     ↓                                            │
[토킹 영상 결과 저장] ────────────────────────────┘

[토킹 클립 순차 생성 루프] → (Port 0: 루프 완료) → [최종 결과 집계]
```

---
---

# 문제 2: B-Roll 5개가 전부 제품샷 — 씬 다양성 없음

## 근본 원인 분석

### 원인 A: PRODUCT_KEYWORDS 리스트가 너무 넓다

현재 B-Roll 프롬프트 5개 생성 코드의 PRODUCT_KEYWORDS:

```javascript
const PRODUCT_KEYWORDS = ['제품', '클로즈업', '히어로', 'hero', 'product', 'bottle', 'KINOHI', '키노히', 'santal', '엔딩'];
```

문제:
- `'KINOHI'`와 `'키노히'`가 포함되어 있다 → 디렉터 AI의 broll_prompt에 브랜드명/제품명이 언급되면 **무조건** 제품씬으로 분류된다
- `'제품'`, `'santal'` → 대본 내용에서 제품 관련 단어가 하나라도 나오면 전부 IMAGE_2_VIDEO
- `'엔딩'` → CTA 씬도 제품씬으로 분류
- 결과적으로 **5개 전부 IMAGE_2_VIDEO (제품 이미지 기반)로 생성** → 전부 비슷한 제품 히어로샷

### 원인 B: 서브 에이전트들의 broll_prompt가 제품 중심으로만 생성된다

디렉터 AI의 5개 서브 에이전트(hook, problem, discovery, solution, CTA)가 broll_prompt를 생성하는데, 이 프롬프트들이 **전부 제품을 보여주는 장면**만 묘사하고 있다.

예를 들어:
- hook: "키노히 바디미스트 병이 빛을 받으며 놓여있는 장면" → 제품샷
- problem: "건조한 피부 위에 키노히 제품이 놓여있는 장면" → 또 제품샷
- discovery: "키노히 바디미스트 언박싱 장면" → 또 제품샷
- solution: "키노히 바디미스트를 손목에 뿌리는 장면" → 또 제품샷
- CTA: "키노히 바디미스트 제품 엔딩 히어로샷" → 또 제품샷

**모든 씬의 broll_prompt에 '키노히', 'KINOHI', '바디미스트', '제품' 같은 단어가 포함** → PRODUCT_KEYWORDS 매칭 → 전부 IMAGE_2_VIDEO → 전부 같은 제품 이미지 기반 영상

## 올바른 B-Roll 씬 구성 (30초 UGC 리뷰 영상)

UGC 리뷰 영상에서 B-Roll은 **토킹 헤드(인물이 말하는 장면)와 교차 편집**되는 보조 영상이다. 각 씬의 B-Roll은 다음과 같이 **다양**해야 한다:

| 씬 | 시간 | 토킹 헤드 내용 | B-Roll (보조 영상) | 생성 타입 |
|-----|------|---------------|-------------------|-----------|
| **Hook** | 0-6s | "아 이거 진짜 대박인데..." | 한국인 여성이 거울 앞에서 스킨케어 루틴 중인 장면, 밝은 자연광 욕실 | **TEXT_2_VIDEO** |
| **Problem** | 6-12s | "건조한 피부 때문에 진짜 힘들었거든" | 건조한 피부 클로즈업, 각질이 일어난 팔뚝/다리, 자연광 | **TEXT_2_VIDEO** |
| **Discovery** | 12-18s | "근데 이거 쓰고 나서 완전 달라졌어" | 제품 언박싱 또는 제품 텍스처 클로즈업 (미스트가 뿌려지는 장면) | **IMAGE_2_VIDEO** |
| **Solution** | 18-24s | "향도 진짜 좋고 촉촉해" | 제품을 피부에 뿌리는 손목 클로즈업, 피부에 수분감 보이는 장면 | **IMAGE_2_VIDEO** |
| **CTA** | 24-30s | "진짜 한번 써봐 후회 안 해" | 만족스러운 표정의 여성이 제품 들고 미소짓는 라이프스타일 장면 | **TEXT_2_VIDEO** |

핵심: **5개 중 2개만 제품 기반(IMAGE_2_VIDEO)이고, 3개는 라이프스타일/감성 장면(TEXT_2_VIDEO)**이어야 자연스러운 UGC가 된다.

---

## 수정 방법: B-Roll 프롬프트 5개 생성 코드 전체 교체

### 변경 핵심

1. **PRODUCT_KEYWORDS를 삭제**하고, 대신 **section 기반으로 generation_type을 결정**한다
2. Discovery(12-18s)와 Solution(18-24s)만 IMAGE_2_VIDEO로, 나머지 3개는 TEXT_2_VIDEO로 고정
3. 각 section에 맞는 **극사실주의 시네마틱 B-Roll 프롬프트 템플릿**을 강제 주입

### B-Roll 프롬프트 5개 생성 — 새 코드 (전체 교체)

`B-Roll 프롬프트 5개 생성` 노드를 더블클릭 → 코드 영역 클릭 → **Ctrl+A** → 아래 코드 **Ctrl+V**:

```javascript
// ============================================
// B-Roll 프롬프트 5개 생성 — v7 (2026.03.05)
// Section 기반 generation_type + 극사실주의 프롬프트 강화
// ============================================

const directorItems = $('Code in JavaScript1').all();
const scriptData = $('대본 파싱').first().json;

const productImageMain = scriptData.product_image_main || '';

// ★ section 기반으로 IMAGE_2_VIDEO / TEXT_2_VIDEO 결정
// Discovery(제품 발견)와 Solution(제품 사용)만 제품 이미지 기반
const IMAGE_SECTIONS = ['discovery', 'solution'];

// ★ 극사실주의 영상 프롬프트 필수 접미사 (CLAUDE.md 규칙 준수)
const HYPERREAL_VIDEO_SUFFIX = 'hyper-realistic footage indistinguishable from real cinema camera capture, ultra-detailed skin with visible pores and micro expressions and natural skin texture movement during motion, realistic hair physics with individual strands responding to movement and gravity and breeze naturally, natural subtle body micro-movements like breathing pulse and muscle tension shifts and weight distribution changes, realistic eye movement with natural blink rate and eye moisture and pupil dilation, fabric physics responding realistically to body movement and air and gravity, natural motion blur consistent with real camera shutter speed, absolutely ZERO artificial smooth plastic skin, ZERO uncanny valley, ZERO AI-generated look, ZERO robotic stiff movement, must pass as genuine footage from professional cinema camera';

// ★ 제품 전용 프롬프트 접미사
const HYPERREAL_PRODUCT_SUFFIX = 'hyper-realistic product photography indistinguishable from real studio photograph, ultra-detailed material textures with visible surface micro-texture and natural light interaction, realistic glass or plastic refraction and reflection with visible internal light caustics, natural shadow falloff with realistic ambient occlusion and contact shadows, visible label printing texture and slight label edge lifting, realistic liquid meniscus visible through transparent container, shot on medium format camera with natural lens characteristics and shallow depth of field, absolutely ZERO CGI render look, ZERO 3D modeling aesthetic, must pass as genuine product photo from commercial studio shoot';

// ★ section별 B-Roll 프롬프트 강화 프리픽스
const SECTION_PROMPT_PREFIX = {
  hook: 'Cinematic 9:16 vertical video, Korean woman in her late 20s doing morning skincare routine in bright natural-lit modern Korean bathroom, warm golden morning light streaming through frosted window, soft steam from recent shower visible in air, she looks into mirror touching her face examining skin, wearing cozy oversized white cotton t-shirt, shot on Sony FX3 with Sony FE 35mm f/1.4 GM at ISO 800 3200K warm tone, shallow depth of field with bokeh on bathroom background, authentic candid moment captured mid-action, ',
  problem: 'Cinematic 9:16 vertical close-up video, extreme close-up of dry flaky skin on forearm and neck area under harsh fluorescent bathroom light revealing texture and dryness, visible dry patches and rough texture, the person gently touches the dry area showing discomfort, natural skin with visible pores and imperfections, shot on Sony A7R V with Sony FE 90mm f/2.8 Macro at ISO 400 5000K neutral daylight, macro-level skin detail visible, ',
  discovery: 'Cinematic 9:16 vertical video, elegant product hero shot with the body mist bottle center frame on marble bathroom counter, soft diffused natural window light creating beautiful caustics through glass bottle, gentle mist spray being released from bottle nozzle in slow motion with visible fine droplets catching light, clean minimalist Korean bathroom aesthetic background, shot on Canon EOS R5 with Canon RF 85mm f/1.2L at ISO 200 4500K, ',
  solution: 'Cinematic 9:16 vertical video, close-up of Korean woman spraying body mist on her wrist and inner arm in slow motion, fine mist particles visible in golden hour sunlight streaming sideways, she gently rubs wrists together then brings to nose with satisfied subtle smile, visible skin moisture and glow after application, soft warm bedroom background with linen curtains, shot on Sony FX3 with Sony FE 50mm f/1.2 GM at ISO 640 3500K warm tone, ',
  cta: 'Cinematic 9:16 vertical video, medium shot of Korean woman in her late 20s holding the product near her face with genuine satisfied smile, warm afternoon golden hour light from window creating rim lighting on her hair, she looks directly at camera with confident happy expression then gives subtle nod of approval, cozy modern Korean apartment living room background with plants, shot on Sony A7IV with Sony FE 35mm f/1.4 GM at ISO 800 4000K, '
};

const brolls = directorItems.map((item, i) => {
  const dir = item.json;
  const section = (dir.section || '').toLowerCase();
  const isProductScene = IMAGE_SECTIONS.includes(section);

  // ★ 원래 broll_prompt에 극사실주의 프리픽스와 접미사를 결합
  const originalPrompt = dir.broll_prompt || '';
  const prefix = SECTION_PROMPT_PREFIX[section] || '';
  const suffix = isProductScene ? HYPERREAL_PRODUCT_SUFFIX : HYPERREAL_VIDEO_SUFFIX;

  // 최종 프롬프트: prefix + 원본 프롬프트 + suffix
  const enhancedPrompt = prefix + originalPrompt + ', ' + suffix;

  // ★ veo_body 조립: section 기반으로 결정
  let veoBody;
  if (isProductScene && productImageMain) {
    veoBody = {
      prompt: enhancedPrompt,
      model: 'veo3_fast',
      generationType: 'IMAGE_2_VIDEO',
      aspect_ratio: '9:16',
      image_url: productImageMain,
      enableTranslation: false
    };
  } else {
    veoBody = {
      prompt: enhancedPrompt,
      model: 'veo3_fast',
      generationType: 'TEXT_2_VIDEO',
      aspect_ratio: '9:16',
      enableTranslation: false
    };
  }

  return {
    json: {
      id: 'broll_' + (i + 1),
      name: dir.broll_name || ('B-Roll ' + (i + 1)),
      section: dir.section,
      time_range: dir.time_range,
      prompt: enhancedPrompt,
      generation_type: isProductScene ? 'IMAGE_2_VIDEO' : 'TEXT_2_VIDEO',
      image_url: isProductScene ? productImageMain : '',
      product_image_url: productImageMain,
      veo_body: JSON.stringify(veoBody),
      brand: dir.brand || scriptData.brand || '',
      product: dir.product || scriptData.product || '',
      full_script: dir.full_script || ''
    }
  };
});

return brolls;
```

---

## 프롬프트 개선 효과 비교

### 변경 전 (현재 — 전부 제품샷)

| 씬 | generation_type | 프롬프트 예시 | 결과 |
|-----|----------------|-------------|------|
| Hook | IMAGE_2_VIDEO | "키노히 바디미스트 제품 등장" | 제품 히어로샷 |
| Problem | IMAGE_2_VIDEO | "건조한 피부에 키노히" | 제품 히어로샷 |
| Discovery | IMAGE_2_VIDEO | "키노히 언박싱" | 제품 히어로샷 |
| Solution | IMAGE_2_VIDEO | "키노히 사용 장면" | 제품 히어로샷 |
| CTA | IMAGE_2_VIDEO | "키노히 엔딩" | 제품 히어로샷 |

### 변경 후 (v7 — 다양한 씬)

| 씬 | generation_type | 프롬프트 핵심 | 결과 |
|-----|----------------|-------------|------|
| Hook | **TEXT_2_VIDEO** | 한국인 여성 거울 앞 스킨케어 루틴, 자연광 욕실, 캔디드 모먼트 | 라이프스타일 |
| Problem | **TEXT_2_VIDEO** | 건조한 피부 매크로 클로즈업, 거친 텍스처, 불편한 표정 | 감성/문제제기 |
| Discovery | **IMAGE_2_VIDEO** | 제품 히어로샷 + 미스트 스프레이 슬로우모션 + 광 투과 | 제품 발견 |
| Solution | **IMAGE_2_VIDEO** | 제품 사용 장면 + 손목에 뿌리기 + 촉촉한 피부 | 제품 사용 |
| CTA | **TEXT_2_VIDEO** | 만족스러운 표정 + 제품 들고 카메라 응시 + 골든아워 | 엔딩/만족 |

---

## 문제 2 수정 체크리스트

- [ ] `B-Roll 프롬프트 5개 생성` 코드를 v7 코드로 전체 교체 (Ctrl+A → Ctrl+V)
- [ ] B-Roll 영상 생성 (Veo 3.1) 노드의 Body가 `={{ $json.veo_body }}` 인지 확인
- [ ] 테스트 실행 후 B-Roll 프롬프트 5개 생성의 OUTPUT에서:
  - Hook, Problem, CTA → `generation_type: TEXT_2_VIDEO` 확인
  - Discovery, Solution → `generation_type: IMAGE_2_VIDEO` 확인
- [ ] 생성된 5개 영상이 각각 다른 느낌인지 확인

---

## 서브 에이전트 프롬프트도 수정해야 하는가?

현재는 **아니다**. 이유:

서브 에이전트(hook, problem, discovery, solution, CTA)가 생성하는 `broll_prompt`는 장면 설명이다. 문제는 그 프롬프트 자체가 아니라, **PRODUCT_KEYWORDS 매칭 로직이 전부를 제품씬으로 분류한 것**이 원인이었다.

v7 코드에서는:
1. PRODUCT_KEYWORDS를 **완전히 제거**했다
2. 대신 **section명(hook/problem/discovery/solution/cta)으로 결정**한다
3. 각 section에 맞는 **극사실주의 시네마틱 프롬프트 프리픽스**를 강제 주입한다

따라서 서브 에이전트가 어떤 broll_prompt를 생성하든, 앞에 붙는 프리픽스가 영상의 톤과 구도를 결정하므로 자연스럽게 다양한 씬이 나온다.

만약 테스트 후에도 영상 품질이나 다양성이 부족하면, 그때 서브 에이전트의 프롬프트를 수정한다.

---

## 전체 수정 요약 (한눈에 보기)

| # | 수정 대상 | 작업 내용 | 소요 시간 |
|:---:|----------|----------|:---------:|
| 1 | `B-Roll 결과 저장` | 새 Code 노드 추가 + IF TRUE와 루프 사이에 연결 | 2분 |
| 2 | `토킹 영상 결과 저장` | 새 Code 노드 추가 + 토킹 결과 조회와 루프 사이에 연결 | 2분 |
| 3 | `최종 결과 집계` | v7.1 코드로 전체 교체 (Static Data 방식) | 1분 |
| 4 | `B-Roll 프롬프트 5개 생성` | v7 코드로 전체 교체 (section 기반 + 극사실주의) | 1분 |
| 5 | 연결 확인 | 위 구조도대로 노드 연결 확인 | 2분 |

**총 소요 시간: 약 8분**
